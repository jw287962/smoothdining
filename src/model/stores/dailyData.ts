// import { Schema, model, Document } from "mongoose";
// const dailyDataSchema = new Schema({
//   date: { type: Date },
//   workers: [{ type: Schema.Types.ObjectId, ref: "Waiters" }],

//   store: { type: Schema.Types.ObjectId, ref: "Store" },
//   // RESERVATIONS / WAITING
//   currentShift: { type: Number }, // represent Shift Number of Current Day
// });

// // hmmm, do I actually need this ???
// export default model("DailyData", dailyDataSchema);
